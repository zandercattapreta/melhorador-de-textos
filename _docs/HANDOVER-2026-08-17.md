---
sistema: MELHORADOR
tipo: sessao
atualizado_em: 2026-08-16
---

# HANDOVER — noite de 16/Ago/2026 (EOD + EOW)

Sessão noturna corrigiu o P0 (U1c) de ponta a ponta. **Aguarda UAT do Zander.**

## Estado

- Branch `feature/backlog-r1-r5-close`, tudo commitado e no remoto.
- Suíte completa verde pela 1ª vez: core 76 lib + OCR real + golden (paridade
  byte a byte nos 4 livros) · app 7. `check-docs` verde.
- Build para UAT: **`_APP/version/2026-08-16_2232/TXTMelhorator.app`**
  (convenção nova: todo build oficial via `bash _APP/scripts/build-release.sh`).

## O que foi corrigido (detalhes no CHANGELOG do dia)

1. GGUF residente + comandos async → sem rainbow wheel, sem recarga por página.
2. Melhorize imediato por página na caixa ao vivo (sempre, mesmo sem LT/IA).
3. `normalize_ocr_page_text`: barras `|`, linhas em branco espúrias e hífens —
   a causa real do "texto cagado" (prova: Schopenhauer I pág. 12).
4. Emissão única por página na extração (consertou teste quebrado).
5. UI: fit da página na moldura; texto acompanha a captura; rótulos.

## P0 da próxima sessão

**UAT do Zander no Schopenhauer I** (e 1 livro nativo). Critério: parágrafos
reais na caixa durante a captura, sem travar, Parar responde, imagem inteira.

## Pendências conhecidas (P1+)

| Pri | Item |
|---|---|
| P1 | Nota de rodapé OCR entra no fluxo do corpo ("NI Este primeiro tomo…") |
| P1 | Hífen na virada de página ("quar-" + "to") — `next_accepts_letter_carry` pronta e testada, falta ligar |
| P1 | U8–U10 (polish DS, fila/biblioteca, diff viewer) |
| P2 | Builds Linux/Windows + GitHub Releases |
| P3 | hOCR tabelas fino |

## Armadilhas (herdadas + novas)

- NUNCA recarregar o GGUF por página ("erro crasso", palavras dele).
- Goldens em `_temp/goldens/` — não apagar; `_temp` NÃO pode ser limpo cegamente
  pelo passo 3 do EOD (goldens moram lá).
- Zander abre o app SEMPRE de `_APP/version/` — nunca apontar para `target/`.
- Demais armadilhas: `HANDOVER-2026-08-16.md` §6.
